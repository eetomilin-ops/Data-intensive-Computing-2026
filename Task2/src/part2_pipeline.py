# Build and fit ML Pipeline

def build_part2_pipeline(tokenizer, stopwords_remover, vectorizer, idf, chi_selector):
    from pyspark.ml import Pipeline
    # TODO: chain transformers and estimators into pipeline
    pass
