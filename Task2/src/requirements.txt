# Task2 Python dependencies
# pyspark 4.1.1 bundles its own Python 3.14 worker -- set PYSPARK_PYTHON
# to the same Python used by the driver (3.12) to avoid version mismatch.
# The shell scripts in src/ handle this automatically.
pyspark==4.1.1
